IF, ID, EX, MEM, WB
MEM and WB are separated because memory is slow and unpredictable
Register writes must be clean, single-cycle, well-timed

MEM - interacts with data memory, finalizes LOAD/STORE instructions, 
passes through ALU results for non-memory ops
LW: read from data memory usding address from EX
SW: write data to memory
*MEM stage does NOT modify reg finalizes
Stores change memory, not registers
Loads retrieve data, but do not commit yet

WB - updates register file, commits results to reg file
selects what gets written (Load data from MEM, ALU result from EX/MEM, CSR result, etc)
selects rd (dest reg), controls reg_write

EXAMPLE: lw x5, 0(x1)
EX - address calculation
MEM - memory read
WB - Loaded value written into x5
**If WB didn't exist as separate stage, forwarding logic chaos

EXAMPLE: sw x5, 0(x1)
EX - address calculation
MEM - memory write
WB: no reg write
**Stores skip WB entirely

EXAMPLE: add x3, x1, x2 (non-memory instruction)
EX - ALU computation
MEM - result just passes through
WB - ALU result written to x3

MEM STAGE PRODUCES RESULTS, WB STAGE COMMITS RESULTS.
--------------------------------------------------------------------------------------------------------------
DATA MEMORY VS. REG FILE
DMEM - larger, slower storage outside datapath
usually, single-ported, synchronous, bigger than reg file
accessed in MEM STAGE only
DMEM never accessed implicitly, only load/store instructions

RF - small storage inside cpu 
feed operands to ALU, receive results from WB, accessed every cycle
accessed in Read: ID stage, Write: WB stage
CPU can use it immediately without waiting
--------------------------------------------------------------------------------------------------------------
R-type (pure register ALU ops)

add x3, x1, x2                  # x3 = x1 + x2
IF - fetch
ID - read x1, x2 from reg file
EX - ALU computation
MEM - Pass-through
WB - write result to x3

key signals:
reg_write = 1
alu_op = ADD 
mem_read = 0
mem_write = 0
--------------------------------------------------------
I-type (immediate arithmetic)

addi x3, x1, 12                 # x3 = x1 + 12
(same as R-type except operand B comes from immediate, not RF)
ID - read x1, sign-extend immediate
EX - ALU uses imm

**imm values are resolved before EX
--------------------------------------------------------
Load instructions (memory -> register)

lw x5, 0(x1)                    # x5 = MEM[x1 + 0]
IF - fetch
ID - Read x1, imm
EX - compute address
MEM - read data mem 
WB - write to x5

key signals:
mem_read = 1
reg_write = 1
wb_sel = MEM

**loads are only instructions that move data from memory into reg file (why load-use hazards exist)
---------------------------------------------------------
Store instructions (register -> memory)

sw x5, 0(x1)                    # MEM[x1 + 0] = x5
IF - fetch
ID - read x1 (addr), x5 (data)
EX - compute address
MEM - write to data memory
WB - nothing

key signals:
mem_write = 1
reg_write - 0

**Stores never write registers
**LOAD/STORE NEEDS TO COMPUTE ADDRESS BECAUSE MEMORY IS NOT ADDRESSED BY REGISTERS 
**IT'S ADDRESSED BY BYTE ADDRESSES
**ex. lw x5, 8(x1)
-> addr = base_register + offset
register = pointer
memory[register + offset]
**telling cpu to add 8 bytes, access memory at that byte address, fetch 32-bit word starting there
**all memory accesses in RISC architecture use effective addr computed by ALU
----------------------------------------------------------
Branch instructions (control flow)

beq x1, x2, label               # if x1 == x2, PC <- target
IF - fetch
ID - read x1, x2, imm
EX - compare + compute target
MEM - often unused
WB - nothing

key signals:
branch = 1
pc_sel = branch_taken ? target: pc+4

**branches don't write to RF, touch memory, but DO mess with pipeline
------------------------------------------------------------
Jump instructions

jal x1, label                   $ x1 = PC + 4, PC = target
IF - fetch
ID - decode
EX - compute target + PC+4
MEM - pass-through
WB - write link register

**jumps write registers, branches don't
------------------------------------------------------------
CSR / special instructions
csrrw x1, csr, x2

--------------------------------------------------------------------------------------------------------------
data memory is byte-addressable
every address refers to one byte (8 bits)
byte at 0x1000 -> 8 bits
byte at 0x1001 -> next 8 bits
byte at 0x1002 -> next 8 bits
A word (32 bits) is just 4 consecutive byte ADDRESSES
ex. lw x5 0(x1)
-> read bytes at:
    address
    address + 1
    address + 2
    address + 3
assemble into 32-bit value.

WHY?
1. different data sizes must coexist
char (1 byte)
short (2 bytes)
int (4 bytes)
double (8 bytes)
structs (mixed)

Registers: no address, sub-address, concept of bytes
x5 = 32-bit value
You can't say x5 + 1 byte.
Registers are identified by name or index (x0-x31)              #x stands for XLEN
                                                                #RV32 -> 32-bit registers
                                                                #x5 = one XLEN-wide value
Accessed as a whole, always read or written in full width

Register's don't need to be byte-addressable because they're meant for computation, not storage layout
Memory is byte-addressed storage, registers are named storage elements with no address space
-----------------------------
task vs function in SV:
functions execute in zero simulation time, behave like comb logic, returns single value
tasks consume time, multiple outputs
ALWAYS do task automatic... or function automatic, or else vars are shared bwtn. calls

tbs shouldn't use same logic to calculate expected values,

rather, use a "golden reference model"
---------------------------------------------------
March 12, 2026
Fetch stage TB kept encountering issues with being out of sync with the DUT:
1. Driver updated interface signals on the rising edge, meaning it took 2 cycles to propagate through the fetch stage's flop. Once driver updated on the negative edge instead, expected PC and model's PC began lining up.
2. Switched from mailbox system to a expected_pc_queue in the scoreboard. Scoreboard needs to calculate next_expected_pc with the current transaction, store in in the queue. Scoreboard uses that for next cycle.
---------------------------------------------------
March 14-16, 2026
Fetch stage Assertion B: PC must not change during stall
1. If stall is asserted in cycle(n-1), PC must hold between cycle(n-1) and cycle(n)
urandom is not supported in xsim, so I used xsim's command-line seed (-sv_seed 12345)
Needed to constrain rand PC values in fetch_txn to be word-aligned.
---------------------------------------------------
March 17-18, 2026
Tests relied on fixed delay to finish (unreliable)
Introduced num_checked in scoreboard and num_txns_sent in env
Env waits until num_checked == num_txns_sent
Mid-test equality issue when comparing scoreboard.num_checked and driver.num_sent
So moved num_txns_sent ownership to env; now the wait only occurs after env finished sending transactions
---------------------------------------------------
March 19, 2026
Before: Driver updates signals on negedge. Monitor samples on posedge. Scoreboard compares current PC with expected PC from a queue that was calculated last cycle.
Going to attempt a single-cycle checking model: driver drives on posedge, monitor samples right after.
Issues: Monitor sampling still seemed to be sampling before fetch stage flop's NBA update. Add real time delay after clocking event:
default input #1step output #0;
Went back to negedge driving 
Added handshake between driver-monitor so monitor only samples PC exactly when txn has been driven, preventing scoreboard from pairing new txn with an earlier queued PC sample.
Monitor still reading clocking block sampled value with vif.cb.pc, need to change to vif.pc
---------------------------------------------------
March 20, 2026
Fetch reset test is not thorough enough. Reset does not go high in between transactions.
Once that was implemented, test fails because scoreboard does not account for resets in its virtual interface.
Fixed scoreboard and reset test thoroughness. But then Assertion D began firing because if assertion and reset occur on the same edge, assertion might observe before reset goes high.
So, changed assertion to check one cycle after reset asserted ($past).
Updated fetch_tb.sv so that reset assertions are displayed in simulation:
reset_prev is necessary to detect on edge only
---------------------------------------------------
March 23, 2026
Need to fix the fact that each test has its own top module, meaning it has to compile, elab, and simulate every test.
This is inefficient and unscalable. Need to compile and elab only once, and simulate many times.
Previously: each test instantiated an env and fetch_tb, and fetch_tb instantiated clock, DUT, and VIF
New single tb_top.sv: instantiates clock, env, VIF, DUT, and creates test object via factory
New fetch_base_test.sv: defines resets, env.wait_for_completion(), which is called by test
Each test in tests/fetch is now a class instead of a module that extends fetch_base_test, and has a run() tasks that sends the transactions
Meaning VIF also is referenced in each test now and not just the top tb.
tb_top calls env.run() and test.run() (through run_test(testname))
Makefile now only needs to compile once per build, and can simulate multiple times. tb_top just calls whichever test was passed and it sends different transactions.
Regression script now creates a single build.log for compilation, and a sim.log for each test.
Boom. Scalable.
---------------------------------
"protected" -> accessible within class and subclasses
"public" -> fully accessible
"local" -> same class only
"virtual" -> allows derived/child class to override method from base (parent) class
"automatic" -> allocating variables on the stack
-----------------------------------------------------
March 24, 2026
To better mimic UVM:
Need to remove coupling btwn. driver and monitor, make monitor passive
Right now monitor only samples after a transaction has been sent by driver. It needs to sample EVERY posedge (especially for reset tests).
Driver: removed drv_done mailbox and driver-monitor handshake
Monitor: remove drv_done mailbox, now samples DUT every posedge. It samples EVERY signal now into an "obs" object, not just PC.
Env: remove drv_done mailbox
Scoreboard: Observe "obs" now and not just DUT pc (will allow to check PC + 4 later)
Good because separates stimulus from observation.
Consumes "obs" until sampled DUT control signals match transaction sent by driver
Interface: Clocking block used to sync passive monitor
-----------------
Added scoreboard and assertions to check PC+4.
-----------------------------------------------------
March 25, 2026
Tests (in fetch_base_test.sv) reports directly from env.scoreboard.
Need to abstract and decouple (not reusable).
Moved completion tracking and end-of-test coordination logic into environment.
Now tests just call env.wait_for_completion() and env.report_results().
Sync helper: env.wait_until_checked().
-----------------------------------------------------
March 26, 2026
Need to clean up reset test semantics so reset-only behavior is separate from optional post-reset bubble behavior.
Tests should not interact with the VIF directly. Need to create method in the env.
Add "txn_tag" in the interface and obs object, so that scoreboard can line up txns with the corresponding sampled DUT cycle.
Scoreboard now compares expected PC to sampled obs object by the monitor, instead of transaction sent.
Fix Assertion C to check for reset as well as stall and pc_src.
drive_idle_controls() is necessary in fetch_reset_test to force a benign post-reset bubble; otherwise stale pre-reset controls can advance PC before the next transaction takes effect.
"reset deassert alone does not clear control inputs
the first post-reset cycle can still see stale controls
this particular test expects a clean restart from PC=0
therefore it needs an explicit idle bubble"
--------------
Add reset injections in fetch_random_test.
Add fetch stall test.
------------------------------------------------------
March 27, 2026
Added decode testbench.
Still need to add assertions.
Main differences: scoreboard calculates expected values using txns now instead of obs objects, and compares that against monitor's sampled obs objects.
Added a light sequencer instead of env sending txns directly to driver and scoreboard.
------------------------------------------------------
March 31, 2026
Decode testbench contains a fatal flaw: randomizing imm_src_de_i
So, created decode_slice testbench that includes main_decoder.sv (skipping control_unit.sv for now)
Then, tb_control_unit, then finally, tb_fetch_decode_control
----------------------
Assertions for decode_slice:
Only funct7_de_o[5] is only driven because it's the only bit that matters:
funct7[5] == 1 → SUB
funct7[5] == 0 → ADD
So don't check for entire funct7.. rest of bits remain X/Z
------------------------------------------------------
April 1, 2026
Added control unit testbench. 
At first I included global signals like reset, clock, and cycle in virtual interface.
Since control unit is purely combinational, not necessary.
To sync driver and monitor, added a handshake via mailbox and manual delays.
----------------------
Replaced manual delays with delta cycle (#0)
Problem: monitor is not passive anymore (handshake)
Need to add valid signal in between txns.